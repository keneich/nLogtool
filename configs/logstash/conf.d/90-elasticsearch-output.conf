output {
  elasticsearch {
    hosts => ["https://elastic01:9200"]
    ssl_enabled => true
    ssl_certificate_authorities => ["/etc/logstash/certs/ca.crt"]
    api_key => "${ES_API_KEY}"
    data_stream => true
  }
}
