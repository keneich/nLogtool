# 가공(파싱/정규화) 파이프라인 확장 지점.
# Elastic Agent가 채워주는 data_stream.dataset 값으로 분기해 필요한 통합/로그 종류별 필터를 추가한다.
# 예시(필요 시 주석 해제 후 수정):
#
# filter {
#   if [data_stream][dataset] == "custom.myapp" {
#     grok {
#       match => { "message" => "%{TIMESTAMP_ISO8601:ts} %{LOGLEVEL:level} %{GREEDYDATA:msg}" }
#     }
#   }
# }
